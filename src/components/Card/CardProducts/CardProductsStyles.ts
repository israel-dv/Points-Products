import { StyleSheet } from 'react-native'

export const cardProductsStyles = StyleSheet.create({
  container: {
    backgroundColor: 'white',
    borderRadius: 10,
    height: 350,
    width: '100%',
    paddingVertical: 20,
    paddingHorizontal: 10,
  },
})
